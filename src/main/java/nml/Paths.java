package nml;

public class Paths {

    /**
     * The local-application.properties file, should be a copy of the template-application.properties,
     * but renamed as: local-application.properties
     *
     * This has been added to .gitignore, in order to avoid pushing local configurations into the master branch.
     */
    public static final String LOCAL_APPLICATION_PROPERTIES_FILE = "template/local/local-application.properties";
}
